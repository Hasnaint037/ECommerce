name: PR Merged Message

on:
  pull_request:
    types: [closed]
    branches:
      - dev

jobs:
  say-hello:
    if: github.event.pull_request.merged == true
    runs-on: ubuntu-latest
    steps:
      - name: Say Hello
        run: echo "Hello world! Your PR is merged 🎉"
