name: PR Merged Message

on:
  push:
    branches:
      - dev
      - main

jobs:
  say-hello:
    runs-on: ubuntu-latest
    steps:
      - name: Say Success Message
        run: echo "This is the PR merged success message 🚀"
